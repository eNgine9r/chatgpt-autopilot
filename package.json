{
  "name": "chatgpt-project-autopilot",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Standalone Raspberry Pi ChatGPT project auto-continue watcher with USER_ACTION_REQUIRED safety gate and Telegram alerts.",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node src/index.mjs",
    "login": "node src/login.mjs",
    "test": "node --test test/*.test.mjs",
    "check": "node --check src/index.mjs && node --check src/login.mjs && node --check src/browser.mjs && node --check src/notifier.mjs"
  },
  "dependencies": {
    "playwright": "^1.55.0"
  }
}
